[project]
name = "smart-finance-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.uv.workspace]
members = [
    "mcp_servers/math_server",
    "mcp_servers/investment_server",
]
